<UserControl x:Class="InventoryManagement.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InventoryManagement.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:InventoryViewModel />
    </UserControl.DataContext>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <Button Content="Làm mới" Command="{Binding LoadCommand}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <DataGrid ItemsSource="{Binding InventoryItems}" SelectedItem="{Binding Selected, Mode=TwoWay}" AutoGenerateColumns="False" Grid.Column="0" Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" />
                    <DataGridTextColumn Header="Warehouse" Binding="{Binding Warehouse.Name}" />
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1">
                <TextBlock FontWeight="Bold" Text="Điều chỉnh tồn kho" Margin="0,0,0,6" />
                <TextBlock Text="Số lượng" />
                <TextBox Text="{Binding AdjustQuantity, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Lý do" />
                <TextBox Text="{Binding AdjustReason, UpdateSourceTrigger=PropertyChanged}" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Nhập (tăng)" Command="{Binding IncreaseCommand}" Margin="0,0,8,0" />
                    <Button Content="Xuất (giảm)" Command="{Binding DecreaseCommand}" />
                </StackPanel>
                <TextBlock Text="Cảnh báo: tồn thấp sẽ hiện màu đỏ trong future." Foreground="Gray" Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
