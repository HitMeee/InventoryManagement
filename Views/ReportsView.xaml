<UserControl x:Class="InventoryManagement.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InventoryManagement.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:ReportsViewModel />
    </UserControl.DataContext>
    <Grid Margin="8">
        <StackPanel>
            <TextBlock FontSize="16" FontWeight="Bold">Reports &amp; Exports</TextBlock>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Export Products CSV" Command="{Binding ExportProductsCommand}" Margin="0,0,8,0" />
                <Button Content="Export Inventory CSV" Command="{Binding ExportInventoryCommand}" />
            </StackPanel>
            <TextBlock Text="Charts and summaries will be added later." Foreground="Gray" Margin="0,8,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
