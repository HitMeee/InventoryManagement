<UserControl x:Class="InventoryManagement.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InventoryManagement.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:CustomersViewModel />
    </UserControl.DataContext>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <Button Content="Làm mới" Command="{Binding LoadCommand}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding Selected, Mode=TwoWay}" AutoGenerateColumns="False" Grid.Column="0" Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" />
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1">
                <TextBlock FontWeight="Bold" Text="Thêm/Sửa khách hàng" Margin="0,0,0,6" />
                <TextBlock Text="Tên" />
                <TextBox Text="{Binding Editing.Name, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Phone" />
                <TextBox Text="{Binding Editing.Phone, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Address" />
                <TextBox Text="{Binding Editing.Address, UpdateSourceTrigger=PropertyChanged}" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Thêm" Command="{Binding AddCommand}" Margin="0,0,8,0" />
                    <Button Content="Cập nhật" Command="{Binding UpdateCommand}" Margin="0,0,8,0" />
                    <Button Content="Xóa" Command="{Binding DeleteCommand}" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
