<Window x:Class="InventoryManagement.Views.UserFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm người dùng" Height="420" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Username -->
        <TextBlock Grid.Row="0" Text="Tên tài khoản:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtUsername" Grid.Row="1" Height="35" FontSize="14"
                 Padding="10,5" BorderBrush="#CCCCCC" BorderThickness="1"/>

        <!-- Password -->
        <TextBlock Grid.Row="3" Text="Mật khẩu:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
        <PasswordBox x:Name="TxtPassword" Grid.Row="4" Height="35" FontSize="14"
                     Padding="10,5" BorderBrush="#CCCCCC" BorderThickness="1"/>

        <!-- Role -->
        <TextBlock Grid.Row="6" Text="Chức vụ:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
        <ComboBox x:Name="CboRole" Grid.Row="7" Height="35" FontSize="14"
                  Padding="10,5" BorderBrush="#CCCCCC" BorderThickness="1"/>

        <!-- Warehouse -->
        <TextBlock Grid.Row="9" Text="Kho:" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,5"/>
        <ComboBox x:Name="CboWarehouse" Grid.Row="10" Height="35" FontSize="14"
                  Padding="10,5" BorderBrush="#CCCCCC" BorderThickness="1"
                  DisplayMemberPath="Name" SelectedValuePath="Id"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy" Width="110" Height="35" Margin="0,0,10,0"
                    Background="#E0E0E0" BorderThickness="0" FontSize="14" FontWeight="SemiBold"
                    Click="BtnCancel_Click" Cursor="Hand"/>
            <Button Content="Lưu" Width="110" Height="35"
                    Background="#4CAF50" Foreground="White" BorderThickness="0"
                    FontSize="14" FontWeight="SemiBold"
                    Click="BtnSave_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
