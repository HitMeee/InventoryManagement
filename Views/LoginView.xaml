<UserControl x:Class="InventoryManagement.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InventoryManagement.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.DataContext>
        <vm:LoginViewModel />
    </UserControl.DataContext>
    <Grid Background="#F5F7FA">
        <Border Width="380" Height="260" Background="White" CornerRadius="8" Padding="16" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="36" Height="36" Fill="#2E7D32" />
                    <TextBlock Text="  Quản lý kho" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>

                <TextBlock Text="Đăng nhập" FontSize="16" FontWeight="Bold" Margin="0,12,0,6" />

                <TextBlock Text="Tên đăng nhập" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Mật khẩu" Margin="0,6,0,0" />
                <PasswordBox x:Name="pwd" PasswordChanged="PasswordBox_PasswordChanged" />

                <TextBlock Text="Vai trò" Margin="0,6,0,0" />
                <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding SelectedRole, Mode=TwoWay}" Width="200">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="IsEditable" Value="False" />
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="Đăng nhập" Command="{Binding LoginCommand}" Width="100" Margin="0,0,8,0" />
                    <Button Content="Thoát" Command="{Binding CancelCommand}" Width="80" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,12,0,0">
                    <Button Content="(DBG) Liệt kê users" Command="{Binding ShowUsersCommand}" Width="160" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
